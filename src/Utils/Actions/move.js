// import store from '../../store';

export const actionMove = (unitId, fromTileId, toTileId) => {
    console.log('actionMove', unitId, fromTileId, toTileId);
};

export const moveFriendly = (unitId, fromTileId, toTileId) => {
    
};